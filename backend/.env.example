# Database Connection String (Neon)
ConnectionStrings__DefaultConnection=Host=my_host;Database=my_db;Username=my_user;Password=my_pw

# JWT Secret Key (Must be at least 32 characters)
JwtSettings__SecretKey=your_super_secret_jwt_key_here_min_32_chars